#include <klib/kstring.h>
#include "db.h"
#include <sqlite3/sqlite3.h>

sqlite3 *db;

#include <iostream>

using namespace std;

int main(){

  string text = "Hello from C++!";
  cout << text << std::endl;
  return 0;

  
  exit(0);
  return 0;
}
